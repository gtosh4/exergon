use bevy::prelude::*;

pub struct ContentPlugin;

impl Plugin for ContentPlugin {
    fn build(&self, _app: &mut App) {
        // Content asset loaders registered here as systems are implemented.
    }
}
